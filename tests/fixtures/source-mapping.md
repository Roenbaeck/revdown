# Mapping fixture

Unicode: café, 中文, and 😀 survive offsets.

Formatting crosses *emphasis* and **strong text**, plus [a visible link](https://example.com).

Escapes show \*literal stars\* and an entity: &amp;.

Inline code is `const answer = 42` inside a paragraph.

- A list item with repeated text.
- A second list item with repeated text.
- [Improvements.md](./Improvements.md) is a developmental record of the earlier draft, not a current task list.

| Name | Meaning |
| --- | --- |
| alpha | first value |

Math is complete: $E = mc^2$.

```ts
const emoji = "😀";
console.log(emoji);
```
